# This is implmentation of deep seek math in hugging face space 
 `App.py` contains code for just baseline model 
 `backup.py` conatains code for new trained model . 
 copy all content from `backup.py` to `app.py` to use new model .
